# 正式素材升级方案

当前游戏已使用原创程序美术完整运行。本文件是**未来可选升级**，不是要求你补图才能运行，也不是假称这些图片已经生成。

## 统一风格与接入约束

青岚渡的视觉关键词：东方奇幻悬崖、青绿山水、纸感低多边形、纸金月光、暖红披风、轮廓清楚、扁平侧视、轻量而非写实重纹理。角色不能变成 Q 版大头占满屏幕，敌人武器、头部和盾牌状态必须立即能辨识。

调色参考：深夜青 `#101f29`、山色 `#1e4248`、纸金 `#f1e6c8`、药瓶青 `#79d5bc`、精准金 `#f2c66e`、敌方暖红 `#e97765`。图中文字、血条、准星、倒计时、数字、粒子和绳索应继续程序绘制，不烧进贴图。

**接入边界：**下列 `public/art/` 是规划目录，当前代码没有向它请求 PNG。制作素材后，应在 `Renderer` 内加入一次性图片加载/缓存，加载成功时 `drawImage`，失败时继续现有程序绘制。不要仅把不存在的 URL 填进代码就宣称已完成替换。Phaser 入口也可改为 `preload` 加载同一路径；替换美术不得改动 `Game` 的判定坐标和时间。

本地原生 Canvas 图片接入参考：

```ts
// 只在初始化时创建，不要在 render() 每帧创建 Image。
const playerIdle = new Image();
playerIdle.src = './art/player-idle.png';
// 在 actor() 的玩家静止分支中：
// if (playerIdle.complete && playerIdle.naturalWidth) drawImage(...);
// else 保留原有程序绘制。
```

正式角色统一以脚下中点为锚点，避免切换姿态时“飘脚”。敌人头部基准位于脚底上方约 55 逻辑像素；图像中的弱点应对齐该坐标。建议贴图为逻辑尺寸的 2–3 倍，而不是盲目使用 4K。动画各帧共享画布、缩放、脚底/武器挂点，不要逐帧自动裁剪。

以下提示词可独立复制使用。除特别说明，PNG 要真实透明背景，不能把黑白棋盘画进图里。

---

## 01. 旅人静止 / 待投姿态

文件：`player-idle.png`；256×256；透明；朝右；脚下中点约在 (128, 235)。

规划路径：`public/art/player-idle.png`。替换函数：`src/render/Renderer.ts → actor()` 的玩家静止分支。后续可做 4 帧轻微呼吸，身体和脚不位移。

完整提示词：

> 绘制移动端横屏动作小游戏的单个东方绳枪旅人角色精灵。纸感低多边形与精致扁平剪影结合，米金色短斗篷与衣袍、青绿色腰带和护腿、小斗笠、向左飘的暖赭红短披风，右手持细长绳枪，面向右侧的纯侧面。身体比例轻巧、头部不夸张，脸部只保留极少细节。角色站稳，脚底齐平，武器朝右前方。视觉轮廓在缩到约 70 像素高时仍清楚。采用青绿山水游戏的低饱和配色，纸金月光细描边，无写实金属噪点，无卡通厚黑边。256×256 PNG，真实透明背景，角色放中央，脚下中点约 (128,235)，四周安全留白，武器不出界。无文字、数字、UI、边框、地面、阴影背景、logo。

## 02. 蓄力动作序列

文件：`player-charge-sheet.png`；1536×256；透明；6 格，每格 256×256，单行从左到右。

规划路径：`public/art/player-charge-sheet.png`。替换 `actor()` 的 `charging` 分支。蓄力从 0 到 1 对应 0–5 帧，不增加蓄力时长。

完整提示词：

> 制作与“青岚渡”旅人一致的 6 帧蓄力动作精灵表，纯右侧面，米金短袍、青绿护腿、小斗笠、赭红披风、细长绳枪。画面 1536×256，严格六个等宽 256×256 单元，单行排列，各帧角色同样大小、脚底锚点固定在每格 (128,235)。从左到右：自然举枪、微微屈膝、肩部后拉、上身进一步后倾、披风绷起、最大蓄力但仍稳稳站住。持枪手和绳索挂点连续移动，不可换手，不要变成弓箭。统一青绿山水纸感低多边形风格，姿态变化清楚但不夸张变形。透明背景，无分格线、序号、文字、UI、地面与镜头运动。所有武器和布料必须留在对应单元内。

## 03. 出枪 / 拉绳 / 飞渡 / 落地动作

文件：`player-action-sheet.png`；1536×256；透明；6 格、同向、相同锚点。

规划路径：`public/art/player-action-sheet.png`。替换 `actor()` 的 `throwing / pulling / settling` 分支。动作挂点由游戏角色位置控制，不能在图里画整条绳索。

完整提示词：

> 为东方绳枪旅人制作六帧连续动作精灵表，1536×256，每帧 256×256，角色身份和服装完全保持一致：米金短袍、青绿护腿、小斗笠、赭红披风，纯右侧面。六帧分别是：松手投枪的短促反作用、右手抓紧绳索挂点、身体被向右上方牵引、轻盈飞渡且披风向左展开、双脚准备落地、落地轻微屈膝压缩。除第一帧可保留手边短枪尾，其他帧不画悬空标枪，不画跨画面的绳索；只明确抓绳手的位置。纸感扁平低多边形，人物轮廓干净，适合 70 像素高显示。各单元尺度相同，锚点一致，真实透明背景，无文字、编号、UI、地面和外框。

## 04. 普通弓手

文件：`enemy-archer-sheet.png`；1024×256；透明；4 格，朝左。

规划路径：`public/art/enemy-archer-sheet.png`。替换 `actor()` 敌人 `archer` 分支。四格对应待机、准备、拉弓、放箭；预警圈仍程序绘制。

完整提示词：

> 制作青绿悬崖战场小游戏的普通敌方弓手四帧动作图，1024×256 单行，四个 256×256 等宽透明单元。人物面向左，低饱和赭红短袍、暗灰青护腿、简单额带、清晰细长木弓。四帧依次为待机、抬弓、拉满、放箭后的回弹。头部要清楚且无遮挡，作为游戏的爆头弱点；不戴覆盖脸部的头盔。脚底锚点一致，不跨单元，造型为纸感低多边形、扁平侧面，轮廓在缩小后清晰。无发光魔法、血条、倒计时、文字、编号、地面和背景。

## 05. 疾射弓手

文件：`enemy-quick-sheet.png`；1024×256；透明；4 格，朝左。

规划路径：`public/art/enemy-quick-sheet.png`。替换 `actor()` 的 `quick` 分支，不改变 2.35 秒基准预警。

完整提示词：

> 绘制青岚渡敌人“疾射弓手”的四帧小型动作精灵表。1024×256，单行四格，每格 256×256，真正透明。纯左侧面，紧身暗青短装、暗橙黄色绑带、短披肩、轻便小弓，姿态比普通弓手更前倾，轮廓轻快敏捷。四帧为待机、快速举弓、拉弦、放箭，固定脚底锚点和头部高度。纸感低多边形、东方古代悬崖风格，不写实堆纹理，不用现代枪械，头部轮廓易读。禁止画速度线、攻击圈、文字、数字、地面或背景。

## 06. 铁卫

文件：`enemy-armor-sheet.png`；1024×256；透明；4 格，朝左。

规划路径：`public/art/enemy-armor-sheet.png`。替换 `actor()` 的 `armor` 分支。四帧：待机、准备攻击、甲片破损、倒下。

完整提示词：

> 为东方绳枪小游戏制作铁卫敌人的四帧精灵表。画面 1024×256，四格等宽 256×256，透明背景。角色左侧面，灰青色分片胸甲、暗色护腿和暖金铆钉，体型比普通弓手稍宽但高度保持一致。头部明确暴露，不画巨大头盔，不遮住脸侧轮廓；胸甲的保护感与头部弱点必须一眼看懂。四帧是站立守备、举武器蓄力、胸甲出现简洁断裂、轻微后仰倒下。纸感低多边形、低饱和青绿山水配色、统一暖月光。无需血液，无文字、血条、数字、地面、格线。

## 07. 盾卫开合

文件：`enemy-shield-sheet.png`；1536×256；透明；6 格，朝左。

规划路径：`public/art/enemy-shield-sheet.png`。替换 `actor()` 的 `shield` 分支；用前后各 3 格对应守/开，不独立控制判定窗口。

完整提示词：

> 制作青岚渡盾卫敌人六帧精灵表，1536×256，单行六格，每格256×256，角色左侧面，青灰衣甲、哑金椭圆盾、小型远程武器。前三帧为盾牌遮住身体的守势与轻微预备动作，后三帧为盾牌明确侧移、身体与头部暴露、出手攻击；开盾与闭盾必须在缩小后仍有很大轮廓差异。固定脚底与头部锚点，衣服和盾纹保持一致。东方纸感低多边形，不要现代盾牌、写实复杂纹样、爆炸与文字。真实透明背景，无格线、序号、光环、血条、倒计时和地面。

## 08. 游弓移动

文件：`enemy-runner-sheet.png`；1536×256；透明；6 格，朝左。

规划路径：`public/art/enemy-runner-sheet.png`。替换 `runner` 分支。脚步循环只影响美术，不重新移动实体，避免双重位移。

完整提示词：

> 绘制“游弓”敌人的六帧横向轻步动画表，1536×256 单行六格，透明背景。左侧面，淡灰紫短袍、暗青护腿、细长弓，身形修长灵活，头部暴露。动作表现小范围左右巡步中的步态变化，不要在各格中大幅改变人物中心；实体位移会由游戏处理。固定画布与锚点，同样服装和比例，脚步清楚。采用东方青绿山水纸感低多边形风格，低饱和、不写实、无厚黑线。无文字、序号、场景、UI、速度线或边框。

## 09. 逐风将

文件：`enemy-elite-sheet.png`；1536×256；透明；6 格，朝左。

规划路径：`public/art/enemy-elite-sheet.png`。替换 `elite` 分支，三崖追猎复用同一角色；不要增加美术与碰撞不一致的巨大体型。

完整提示词：

> 制作东方悬崖战场中的精英“逐风将”六帧精灵表，1536×256，单行六格256×256，真实透明。角色左侧面，深赭红披肩、青灰分片胸甲、短小赤色冠羽、精炼古代弓；轮廓有将领身份，但体型只比普通兵略宽，不是巨型 Boss。头部弱点暴露且清楚。六帧：威慑待机、蓄势、拉弓、放箭、受击、短促后撤。统一纸金月光、青绿山水低多边形纸感。动作在本格内不裁切，固定脚底锚点。不画旗帜背景、文字、血条、数字、连击效果或绳索。

## 10. 基础悬崖平台

文件：`platform-stone.png`；512×768；透明；非动画。

规划路径：`public/art/platform-stone.png`。替换 `rock()` 的普通崖体缓存。顶面目标宽约 128 逻辑像素；根据需要缩放，不修改平台中心和可达距离。

完整提示词：

> 绘制单个青岚渡悬空石台素材，512×768 透明 PNG。上方是清晰可站立的浅青灰石砖顶面，下方是向下收窄的古老青绿岩柱，岩柱轮廓略不规则但不杂乱，少量岩层折线、苔痕、纸感切面，底部向透明渐隐。纯横屏侧视游戏角度，顶面只露出很薄的俯视厚度，不能变成等距视角方块。色调上亮下暗，暖金月光细描边，低饱和山水风格。无角色、药瓶、旗帜、文字、UI、其他平台或背景，顶部留白清楚，四周不裁断轮廓。

## 11. 穿透阶梯平台

文件：`platform-pierce.png`；768×768；透明；阶梯高度需适配代码。

规划路径：`public/art/platform-pierce.png`。只替换 `drawPlatform()` 的宽平台基座，角色脚下的小台阶仍根据弹道程序绘制，避免图片预画台阶与判定错位。

完整提示词：

> 绘制青岚渡用于多敌穿透局面的宽石台基座，768×768 透明 PNG。石台顶面足以横向站三名小型角色，顶面保持平整，暂不绘制固定小台阶，因为台阶由游戏实时添加。下方为青绿古岩柱与斜向断层，底部逐渐透明。薄顶面侧视、东方纸感低多边形、青灰石砖、少量苔色、纸金细描边，与普通平台保持统一。不要画敌人、武器、光效、药品、文字、UI、地面背景、透视夸张的方盒。

## 12. 灵药瓶

文件：`pickup-heal.png`；128×128；透明；可做 4 帧微光，但不把漂浮位移烘焙进图。

规划路径：`public/art/pickup-heal.png`。替换 `potion()` 中的瓶身，光晕和上下浮动仍由程序绘制。

完整提示词：

> 绘制东方奇幻小游戏的一个小灵药瓶图标，128×128 真正透明 PNG。圆润青绿色半透明小瓶、短颈、浅金木塞、瓶身有简单深青医疗加号形符记。适度手绘纸感与低多边形结合，外轮廓干净，缩到 24 像素也能立刻认出是恢复生命的药。淡暖金边光，无大面积白色高光，不像现代塑料药瓶。瓶子居中且有安全留白，无文字、数量、按钮、边框、地面与投影背景；不要附带巨大光圈。

## 13. 标枪与有效锚点

文件：`spear.png`（256×64）与 `anchor.png`（128×128）；透明。

规划路径：`public/art/spear.png`、`public/art/anchor.png`。替换 `rope()` 枪头绘制与 `anchor()` 的锚牌；绳索必须继续连接真实手部与枪头位置。

标枪完整提示词：

> 绘制单根向右水平的古代细长绳枪精灵，256×64，真正透明背景。哑金木制长杆、纸金浅灰的菱形枪头、左端一个小绳索连接环，轮廓细而明确。东方青绿纸感低多边形游戏素材，不使用火器结构，不画弯曲绳子或轨迹。枪头朝右，整体居中，两端留安全边距，无文字、阴影背景、血液、手或其他物体。

锚点完整提示词：

> 绘制青岚渡游戏的单个可钩挂锚牌，128×128 透明 PNG。青绿色菱形金属环，中央纸金小菱形，底下极短石质连接底座，东方古代简洁器物，轮廓清楚，纸感低多边形，发光克制。只画器物，不画绳索、不画巨大光环、不画平台。无文字、箭头、准星、UI、背景或边框。

## 14. 三层可拼接山景

文件：`mountain-far.png`、`mountain-mid.png`、`mountain-near.png`；每张 2048×720；上半透明；水平无缝衔接。

规划路径：`public/art/` 下同名文件。替换 `makeMountains(0/1/2)`；各层继续按现有独立速度视差滚动。

远层完整提示词：

> 绘制横向无缝衔接的东方青绿山水游戏远景山峦层，2048×720 PNG。远峰位于画面中下部，上部真正透明，用于叠在独立天空之上；低饱和灰青绿色、疏淡纸感、薄雾、折纸般简练山峰轮廓，壮阔安静，局部笔触极少。左右边缘山势自然衔接，不出现明显接缝。纯侧向横屏游戏背景层，无月亮、星空、人物、平台、建筑、文字、UI、边框。

中层完整提示词：

> 绘制 2048×720 可水平无缝拼接的青绿山水小游戏中景层，PNG 上部透明。山峰比远景更饱满、稍深的青绿色，多段悬崖和山脊交错，纸感低多边形轮廓，少量浅青岩层线，不要密集树叶纹理。顶部轮廓变化舒展，不遮挡前景游戏角色，左右边缘接续自然。无天空、月亮、太阳、角色、独立可站立平台、UI、文字与边框。

近层完整提示词：

> 绘制 2048×720 横向无缝青岚渡近景山体层，上半真正透明，深青绿色纸感低多边形山脊，画面下半为稀疏松树剪影和山石，松树大小错落但不密集成墙。适合缓慢横向视差滚动，左右边缘无缝，避免高对比细节抢夺前景敌人与绳索。无天空、月亮、人物、站台、文字、UI和边框。

## 15. 可平铺薄雾

文件：`fog-strip.png`；1024×128；透明；可横向平铺。

规划路径：`public/art/fog-strip.png`。替换 `background()` 的雾带；可继续使用当前渐变程序雾，不必为了换图额外增加文件。

完整提示词：

> 绘制用于东方青绿山水游戏的单条低对比薄雾纹理，1024×128 PNG，真正透明背景，雾色为浅灰青绿色，水平方向柔和绵长，有轻微手绘纸感、边缘自然消散，左右可无缝衔接。纹理要极轻，不要像烟团或暴风云。无物体、山、人物、光束、闪电、文字、边框或黑色底色。

## 16. 可选关卡旗帜

文件：`flag-risk-sheet.png`；768×128；透明；6 帧，每格128×128。

规划路径：`public/art/flag-risk-sheet.png`。替换 `drawPlatform()` 的高风险/追猎旗面，旗杆及锚点可继续程序绘制。

完整提示词：

> 制作青岚渡高风险平台的小旗六帧飘动精灵表，768×128 单行六格128×128，真正透明。只有一面暖赭红三角短旗，带一个很小的纸金菱形标志，旗面固定在每格左侧同一位置，向右轻轻飘动；不要附带旗杆，旗杆由程序绘制。低饱和东方纸感低多边形风格，六帧形变自然、布料不撕裂，不出现大量褶皱纹理。无文字、序号、边框、平台、人物或背景。

## 动画与美术验收

先只替换静止旅人与普通弓手，在真实 1280×720 逻辑布局中检查轮廓、头部判定、武器挂点和脚底；通过后再扩展动作表。生成工具未必能可靠保证精灵表六帧一致，因此可能需要分帧生成、裁切和人工校正，不能仅凭有六个人物就当作可用动画。

不要替换程序化预警圈、HP、力度条、分数字体、轨迹与绳索为图片：这些必须跟游戏状态精确同步。不要为了素材升级更改敌人判定尺寸或弹道容差来掩盖图片锚点错误。
