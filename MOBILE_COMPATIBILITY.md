# 移动端显示策略（v1.5.1）

游戏逻辑坐标始终为 1280×720 横屏。

- 横屏设备：正常按横屏显示。
- 竖屏设备：不弹旋转提示、不暂停，整个 Canvas 自动顺时针旋转 90° 后居中显示。
- 竖屏只是视觉舞台旋转，不改变蓄力、射程、世界生成或计时逻辑。
- 触控坐标会根据 Canvas 的实际旋转矩形自动逆变换回 1280×720 逻辑坐标。
- `viewport-fit=cover` 与 Safe Area 仍保留；典型长屏竖屏会自然形成上下留白，避免刘海覆盖核心游戏画面。
- 不调用 Screen Orientation API，不要求浏览器/系统切换方向。
