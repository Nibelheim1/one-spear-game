# v1.5.0 单按钮蓄力参数

- `chargeSeconds = 1.28s`：0～1.28 秒严格单调线性；满蓄力后封顶。
- `chargeRangeBase = 120`，`chargeRangeScale = 770`：有效水平射程 = `120 + 770 × power`。
- 新生成近/中/远平台基准水平间距：`370 / 590 / 810 px`，随机扰动仅 ±14 px。对应从枪头计算的典型理想力度约 0.30 / 0.59 / 0.87，典型按压约 0.39 / 0.75 / 1.10 秒。
- `hitBand = ±0.115 power`：只有落在合理距离容错内才允许软吸附；明显短投/过投仍坠崖。
- `perfectBand = ±0.030 power`。
- `tutorialHitBonus = 0.065`，仅前三跳增加教学容错。
- 触摸 `x/y` 和 pointer move 不再参与任何路线选择；正式阶段不显示目标准星/高亮，也不提供“精准到了”的触觉暗示。

# v1.4.1 远程攻击当前参数

- `firstRangedAttackDelay = 2.00s`。
- 同一时刻仅 `activeShooterId` 对应的最近存活敌人能够推进远程攻击计时。
- 更远敌人的 `timer` 冻结，保持首次攻击完整窗口；它们不显示攻击进度环。
- 最近敌人被击杀/越过后，新的最近敌人如果尚未出手，其首次攻击计时重置为完整 **2.00s**。
- 后续连续攻击仍保留 `enemyAttackIntervalScale = 0.50` 的 v1.3.1 节奏。
- 残敌近战 `0.16s` 首刀、`0.17s` 连刺不变。

# v1.3.2 首次远程攻击节奏

- `firstRangedAttackDelay = 1.50s`。
- 首次攻击与后续攻击周期拆分。首次倒计时不受 `grace` 延长；后续攻击继续使用敌种 `period` 与 `enemyAttackIntervalScale = 0.50`。
- 教学第一跳弓手：首次出手约 1.50s；第一次出手后 `period = 2.90s`。
- 仍保留绳索飞跃阶段冻结远程攻击计时，避免玩家已经做出不可撤销动作后遭到不公平攻击。

# 核心手感与调参记录

所有核心玩法参数在 `src/config/balance.ts`。这里记录实际实现值，不把需求中的示例值当成已实现值。

## 最终关键值

| 参数 | 值 | 体验目的 |
|---|---:|---|
| 最大生命 | 100 | 承受远程与残敌近战；投空改为坠崖，不走 HP 容错 |
| 药瓶恢复 | 32 | 有救命价值，但不是全恢复 |
| Mock 续命 | 36 | 一局一次；无真实广告 |
| 首次满蓄力 | 1.28 秒 | 近路约 0.63 秒，高路约 0.82 秒，远路约 1.01 秒 |
| 命中力度容差 | 理想值 ±0.115 | 新手能命中；头部奖励另算 |
| 前三次成功跳跃的额外容差 | ±0.055 | 配合固定教学，先建立手感再撤 UI |
| 精准力度容差 | 理想值 ±0.036 | 首轮约 92 毫秒宽的精准窗口 |
| 蓄力→射程映射 | `(power − 0.27) × 1490` 像素 | 同一按住时长永远对应同一水平有效射程 |
| 满蓄力行为 | 1.28 秒后固定为 1.0 | 不回弹、不往返扫条，消除肌肉记忆歧义 |
| 换目标门槛 | 68 逻辑像素 | 轻拖即可，减少无意切换 |
| 投枪飞行 | 0.29–0.51 秒 | 看得见抛物线，但不拖沓 |
| 重力 | 950 像素/秒² | 给出清楚弧线，不做物理考试 |
| 磁吸半径 | 11 像素（头部扫掠另加半径） | 用小幅修正吸收输入误差 |
| 旧收枪参数 | 0.23 秒 | 兼容字段保留；当前投空不再走收枪重试 |
| 绳索飞渡 | 0.29–0.52 秒 | 随距离变，非瞬移 |
| 投空坠崖 | 0.82 秒；前冲 90–620 像素 | 前冲取蓄力有效射程的约 70%，失手也保持“按得越久飞得越远” |
| 飞渡抬升弧 | 74 像素 | 展现“绳子拉我过去”的动作感 |
| 落地等待 | 0.10 秒 | 留压缩/扬尘，不长时间阻断操作 |
| 落地远程攻击保护 | 0.82 秒 | 避免刚落地被下一组远程敌人必打中 |
| 残敌首刀 / 连刺间隔 | 0.16 / 0.17 秒 | 首刀反应窗不变，后续连续刺击频率翻倍 |
| 残敌单体近战伤害 | 9–16 / 次 | 按敌种累加；多人存活时掉血显著加速 |
| 普通/精准/多杀停顿 | 45 / 75 / 110 毫秒 | 反馈分层，多杀比普通命中更重 |
| 弓手基准预警 | 3.00 秒 | 足够观察和蓄力 |
| 疾射基准预警 | 2.35 秒 | 施压但不叠最高伤害 |
| 预警下限 | 2.20 秒 | 长局仍维持可理解的时间窗口 |
| 路线预警错峰 | 每层额外 0.38 秒 | 避免所有敌人同步结算伤害 |
| 教学敌人额外预警 | 约 +2.8 秒 | 留给前三跳读懂单人、多杀和回血提示 |
| 来箭基础飞行 | 0.70 秒，加距离量 | 给“最后一刻拉走”留下机会 |
| 受伤保护 | 0.50 秒 | 不让同一瞬间多个箭头重叠扣血 |
| 平台距离中心 | 355 / 580 / 805 像素 | 远近力度和价值可区分 |
| 三类平台高度范围 | 近约 400–510，高约 290–365，远约 515–572 | 上下轻拖的空间意义明确 |
| 连跳计分 | 1 → 2 → 4 → 8 → 16 → 32 | 每次成功飞渡翻倍，32 封顶 |
| 连跳打断 | 敌人完成一次远程/近战攻击 | 下一次成功飞渡重新从 1 分开始；不再按等待时间自动消散 |
| 越台奖励 | +32 | 越过最近台，直接飞到第二个平台 |
| 粒子 / 浮字池 | 150 / 22 | 固定上限 |
| 历史平台保留 | 5 | 无限世界不无限积累 |

## 具体迭代

### 1. 将复杂角度控制改为“距离对应力度窗口”

玩家只负责目标与时机，角度/弹道求解交给系统。实现了真正可见的抛物线与扫掠命中，但不用额外角度摇杆。源码检查及浏览器实际首投验证了按住到金线、松手、击破和飞渡闭环。

### 2. 蓄力改成单调线性，建立可学习的肌肉记忆

v1.1 的满蓄力后回落虽然允许“蓄过头再等回来”，但它也意味着同一长按时长在后半段可能对应不同力度，不适合撤掉力度条后的纯手感玩法。v1.2 取消回弹：`power = clamp(按住时间 / 1.28 秒, 0, 1)`；有效水平射程再按统一线性函数换算。命中目标时，目标距离使用同一函数反推理想蓄力；投空时标枪实际终点和角色坠崖前冲也使用这套射程，因此视觉结果和玩家肌肉记忆一致。

### 3. 把穿透几何做成台阶，不随机赠送双杀

同平台多敌的站立位置沿首次解算的标准抛物线微调，并绘制对应台阶。单支飞矛以线段扫过头/身体，保留命中列表避免一敌重复扣分。核心测试实际验证两敌、三敌同矛命中。

### 4. 让出箭后移动真的能闪开

来箭的终点锁定发射时玩家位置，而非永远追踪。绳索飞行不是只播放动画；玩家坐标真实改变，箭抵达旧位置会落空。用落地保护和错峰预警防止刚到新平台就被不可读的伤害打中。

### 5. 修正粒子加速度单位

画面检查发现命中火花出现异常向下长尾。原因是粒子重力按“每帧加 160”而不是按秒乘 `dt`。修复后，重力以秒计，环形冲击波不再受重力下坠；火花留在击破附近，避免视觉噪声。该修复后重新开始完整的十分钟连续运行检查。

### 6. 多杀浮字去重

初版每名敌人都弹一整份大号 PERFECT，双敌间距较小时文字互相盖住。改为一投只给一份主反馈，敌人分数错位出现；穿透总奖励转为底部短通知，大字保留“一矛双杀/三杀”。

### 7. 模态 UI 与 Seed 边界

暂停、帮助、结算界面重新设置可点击区域，防止点到后方战斗按钮。数字 Seed 直接作为无符号种子而非再次字符串散列，因此调试面板显示的数值可以直接复制重现。

## 验证不等于主观结论

这些修改来自代码检查、浏览器实际交互、逐帧/截图观察和自动化数据，**不是一组真实玩家访谈**。当前的“爽感”和难度曲线是经过内部检验的设计判断，仍要在目标手机上试听、触控试玩并观察真实新手首分钟表现。

下一轮最有价值的是找 3–5 名未看说明的人各玩 5 分钟，记录首投成功、是否主动选药、是否理解盾窗，以及死亡后的重开意愿，而非继续堆测试覆盖率。此项尚未执行。


### 8. v1.1：把“命中”改成真正的生死承诺

按用户反馈覆盖原先“投空收枪”的容错：标枪未扎中、被盾弹开或没有形成有效挂点时，玩家仍然向前跃出，约 0.82 秒后坠崖死亡。这样长按—松手不再是可以无限试错的瞄准按钮，而是一次明确承诺。

同时取消蓄力阶段的抛物线引导点，只保留目标高亮与力度条。多人平台也从“系统要求清场后才允许飞”改为“扎中就飞”：若一枪没有全杀，残敌落地后约 0.16 秒开始近战，约每 0.34 秒连续结算，单体伤害 9–16，多个残敌按人数/敌种累加。浏览器实测故意让双人台保留 1 名铁卫时，约 0.8 秒累计受到 42 点近战伤害。

### 9. v1.2：三跳教学后撤掉力度 UI

前三次成功跳跃固定为单人台、双人穿透台、回血台。三次都显示力度条并给出对应教学提示；第三次成功落地后，普通模式不再绘制下方力度条，也不再用准星颜色泄露“现在是否进入命中/精准窗口”。玩家仍能看清选中的目标，但必须根据距离与按住时长自行判断。

### 10. v1.2：已经看见的平台不再重抽

旧实现每次落地直接替换 `platforms` 数组，导致前一秒看见的远端双人台/回血台在镜头推进后变成另一种内容。现在落地时保留所有仍在前方的已显示平台对象，只从生成器补充更远平台。浏览器流程记录落地前平台 ID、kind 和敌种数组，并在落地后逐项比对，已显示对象保持不变。

## v1.3.1 攻击节奏

- `enemyAttackIntervalScale = 0.50`：应用于世界生成后的最终远程攻击周期，所有敌人预警/重复出手间隔均为 v1.3 的一半。
- 教学第一跳弓手的初始周期由约 5.8s 变为约 2.9s；常规弓手典型周期由约 3.0s 变为约 1.5s（难度与平台错位仍会造成小幅差异）。
- `meleeInterval = 0.17s`；`meleeFirstDelay = 0.16s` 保持。
- 总分 HUD 主数字 52px，金色粗体，带独立暗底金边面板。


## v1.4.0 display invariants
The logical world remains 1280×720 at every device size. No aspect-ratio stretch is allowed, specifically to preserve the linear charge-time-to-range feel. Ultra-wide screens use decorative side bleed rather than a wider gameplay coordinate system.
