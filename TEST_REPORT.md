# v1.9.0 定向验证

- JS bundle `node --check` 通过。
- GREAT弓手：可击杀；GREAT铁卫：不能击杀；PERFECT逐风将：可击杀；闭盾PERFECT仍被盾挡。
- 击杀加分：弓手+2、精英+5已由实际ScoreSystem验证；与平台基础分/Combo为加法关系。
- 2000m前300组种子未出现流动平台；2500m后的300组种子出现16组流动台，均为无敌人、基础20分、`42px/s`、`±34px`。
- 匀速反射预测测试：平台从中心向右以42px/s运动1秒，预测X=+26px（先到+34边界再反向8px），符合匀速往返。
- 移动平台投矛会使用未来X锚点；测试中`shot.end.x != 当前anchorX`，确认存在提前量。
- Game级测试：站在移动平台时，1/60秒后平台前进0.7px，玩家X同步前进0.7px；Y保持不变。

# v1.8.1 定向验证

- 回血：任意低于100 HP的状态落地回血台后都直接变为100；统计的healed仅记录实际恢复差值。
- 基础分：平台只允许1/5/10/20四档；回血=5、风险/穿透=10、Boss=20。
- Combo叠加：普通1分台连续PERFECT仍为1/2/4/8/16/32；10分台对应10/11/13/17/25/41，20分台对应20/21/23/27/35/51，不进行乘法爆分。
- 越台：额外+32仍独立叠加。
- 回血路线概率已调整为12%/20%/42%/60%，近期受伤救济下限50%。

# v1.8.0 定向规则验证

- 已检查三份运行入口（根 `index.html`、`one-spear-play.html`、`dist/index.html`）规则代码一致。
- PERFECT 命中：敌人立即进入死亡状态，`kills` / `heads` 记录增加。
- 非 PERFECT 命中：不降低敌人生命、不击杀，但仍进入有效挂绳/飞渡流程；落地后存活敌人进入近战压力。
- Combo：连续 PERFECT 才递增；非 PERFECT 成功飞渡重置为0并只获得1基础分；受伤/敌人出手不再调用 Combo 重置。
- 近战新值：0.34s首刀 / 0.30s间隔 / 多人权重100%-65%-45%。
- 小兵方向检查：`melee_idle`、`melee_attack`、`shield_idle`、`shield_defend`、`ranged_aim`、`ranged_shoot` 六种运行素材均朝左；其中三张通过镜像原图修复。
- 实际 bundled 模块测试通过：Perfect→Combo 1、连续 Perfect→Combo 2、非 Perfect→Combo 0且基础+1、随后 Perfect 从Combo 1重新开始；非 Perfect 敌人命中保持存活，Perfect 头部命中立即击杀，闭盾仍会弹枪。


## v1.7.1 蓄力音效专项

- 静态语法检查：三份运行入口的主 bundle 均通过 `node --check`。
- 每次 gameplay pointer down 都显式调用 `audio.startCharge()`；不是依赖上一次 `chargeOsc` 的残留状态。
- 固定层级时间为 0.00 / 0.30 / 0.60 / 0.90 / 1.20 秒，使用 WebAudio 绝对时间一次性排程。
- `stopCharge()` 同时终止基础振荡器、谐波振荡器与尚未触发的层级 cue，避免短按后未来提示音仍播放。
- 根目录 `index.html`、`one-spear-play.html` 与 `dist/index.html` 的游戏代码保持同步。
- 当前环境无法通过物理手机扬声器进行主观听感 AB 测试，因此“音色是否最悦耳”仍需真机试听；本轮验证的是时间层级、重置机制与代码链路。

# TEST REPORT — v1.7.1 数值 / 蓄力音效精修

- 代码静态检查：`index.html`、`one-spear-play.html`、`dist/index.html` 三份运行入口参数一致。
- 近/中/远典型按压重新计算为约 **0.387 / 0.753 / 1.162s**。
- 远档 ±14px 范围（822～850px）对应理想力度约0.890～0.926，**1.28s满蓄与理想值误差均小于正式 ±0.115 命中容差**。
- 正式普通命中总时间窗约294ms；Perfect总时间窗约90ms；三次教学总命中时间窗约448/384/333ms。
- v1.8 当前近战：普通弓手1/2/3人每轮约6/10/13伤害，0.30s连刺；首刀延迟0.34s。
- 后续远程攻击周期加 `max(1.25s, 原周期×0.5)` 硬下限。
- 动态回血目标概率为高血18%、中血30%、低血58%、危急70%；近期受伤跳≥2时至少58%。
- 移除“险路×1.8”显示和基于miss次数的局内救济。

# TEST REPORT — v1.6.0

## 1. 静态构建检查

- HTML 内嵌 JS 使用 Node `--check` 通过。
- 项目图片素材 82 个，RGBA PNG 均可正常解码。
- 运行入口：`index.html` / `one-spear-play.html`。

## 2. 核心逻辑回归

真实执行嵌入模块后验证：

- 首次远程攻击窗口：`2.00s`。
- 蓄力保持线性：`power = clamp(holdSeconds / 1.28, 0, 1)`。
- Combo 得分：`1, 2, 4, 8, 16, 32, 32, 32...`。
- 越过第一台直达第二台：额外 `+32`。
- 100 个 seed 的正式平台宽度范围约 `92.43–229.24`，存在 normal / pierce / heal / risk / safe 多类型。
- 只允许最近存活远程敌人计时和射击；更远敌人的计时保持冻结。

## 3. Renderer 实际运行检查

使用 Skia Canvas 执行与浏览器相同的 Game / Renderer 模块并输出真实渲染帧：

- 菜单：`docs/menu-v1.6.png`
- 多平台正式玩法：`docs/gameplay-v1.6.png`
- Combo + 越台加分：`docs/scoring-v1.6.png`
- 结算页：`docs/results-v1.6.png`

检查结果：

- 主角、敌人、平台、HUD 图片素材均成功加载。
- 不同平台的尺寸和轮廓在同一画面内可明显区分。
- 总分常驻视觉层级高于次要数据。
- `越台 +32` 与本跳分数分层显示，没有混成一个虚高数字。
- 第四跳以后不显示力度条和目标准星。

## 4. 移动端方向逻辑

沿用 v1.5.1：

- 不阻塞竖屏进入。
- 竖屏时整个 16:9 舞台旋转 90° 显示为横屏方向。
- 触控坐标使用逆变换映射。
- Safe Area / VisualViewport 逻辑保留。

## 5. 边界说明

本环境的系统 Chromium `--screenshot` 进程仍存在退出挂起问题，因此没有把 Chromium CLI 截图冒充成通过；本版使用同一 JS 游戏模块 + Skia Canvas 直接执行 Renderer 完成画面回归，JS 语法与核心逻辑另行独立验证。

## 6. dist 静态部署验证

- `dist/index.html` 与正式入口一致，并携带完整 `assets/`。
- HTML 实际引用 28 个运行时 PNG；构建前逐一检查本地文件存在，缺失数为 0。
- 使用 Python 静态服务器从 `dist/` 启动后，根路径返回 HTTP 200，返回的 `index.html` 与构建文件逐字节一致。
- 对 28 个运行时图片 URL 逐一 HTTP 请求，均返回 200。
- `dist/` 共 85 个文件（含 82 个 PNG、入口、`.nojekyll`、`build-info.json`）。


## v2.0.0 视觉降噪回归

- 三个运行入口 `index.html` / `one-spear-play.html` / `dist/index.html` 采用同一份 bundle。
- JS 语法检查通过；未改动游戏状态机和数值配置字段。
- 背景/普通平台/HUD 的视觉降噪通过参数检查：背景平均HSV饱和度由约0.611降至约0.430；普通平台运行时约0.70饱和；高价值平台保留约1.0。
- 得分反馈按低/中/高三档分流，越台、20分台、高阶Perfect Combo仍保留完整高光。
- 当前沙箱的系统 Chromium 对 localhost/file URL 启用了组织策略拦截，因此无法把该浏览器截图作为实际视觉验收证据；该限制不影响静态包、JS语法与资源路径检查。
